import 'app_sizes.dart';

class AppRadius {
  const AppRadius._();

  static const double sm = AppSizes.radiusSm;
  static const double md = AppSizes.radiusMd;
  static const double lg = AppSizes.radiusLg;
  static const double pill = AppSizes.radiusPill;
}
